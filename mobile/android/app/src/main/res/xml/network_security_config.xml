<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted only for the staging host and the emulator alias.
  android:usesCleartextTraffic="true" would allow it for every destination,
  including production. Delete these entries once staging has TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">54.179.40.243</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
