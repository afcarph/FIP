[pytest]
testpaths = tests
# The modules are flat at the project root, which is how they are imported in
# the container. Tests need the same path, and conftest adds it.
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:parser
