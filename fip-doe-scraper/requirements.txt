# FIP DOE scraper — runtime dependencies.
#
# Ranges rather than exact pins: the lock lives in the Docker image, which is
# built from this file and then pinned by digest. Exact pins here would mean a
# security patch to any of these needs a code change.

# --- browser automation -----------------------------------------------------
# Playwright drives a real Chromium so the dashboard's own JavaScript issues the
# batchedDataV2 requests. We read the responses; we never touch the DOM.
playwright~=1.49

# --- HTTP -------------------------------------------------------------------
# Used for the schema fetch and for replaying a captured request without paying
# for a browser start-up.
requests~=2.32

# --- persistence ------------------------------------------------------------
SQLAlchemy~=2.0
PyMySQL~=1.1
# PyMySQL needs this for MySQL 8's default caching_sha2_password auth.
cryptography~=44.0

# --- transformation ---------------------------------------------------------
pandas~=2.2

# --- configuration ----------------------------------------------------------
pydantic~=2.10
pydantic-settings~=2.7
python-dotenv~=1.0

# --- scheduling and resilience ----------------------------------------------
APScheduler~=3.11
tenacity~=9.0
