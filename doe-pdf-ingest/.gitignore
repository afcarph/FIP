.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Secrets.
.env

# Run output. The stored PDFs are the DOE's own published documents, kept so a
# past week can be re-extracted — they belong on a volume, not in git.
logs/
storage/
